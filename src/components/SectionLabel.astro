---
interface Props {
  label: string;
  eyebrow?: boolean;
}

const { label, eyebrow = false } = Astro.props;
---

{eyebrow ? (
  <p class="text-xs font-semibold uppercase tracking-widest text-muted mb-2">{label}</p>
) : (
  <h2 class="text-2xl font-semibold mb-4">{label}</h2>
)}
